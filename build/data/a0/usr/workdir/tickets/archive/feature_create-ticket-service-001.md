# Ticket: Create TicketService for Markdown Parsing

**Type:** Feature
**Branch:** `feature/create-ticket-service-001`

## Description
Develop a new `TicketService` class that can read and parse `.md` (Markdown) files. The service should be robust and cleanly implemented.
Ensure high code quality adhering to the project's standards, including strict types and tests.

## Acceptance Criteria
- [x] Create `TicketService` class to read and parse markdown files.
- [x] Ensure strict typing (`declare(strict_types=1);`).
- [x] Unit tests are written and pass successfully (using Codeception).
- [x] PHPStan level 9 passes with zero errors.

## Developer Instructions
⚠️ **MANDATORY:** You must check out the branch `feature/create-ticket-service-001` before starting work. All commits must be pushed to this branch.
